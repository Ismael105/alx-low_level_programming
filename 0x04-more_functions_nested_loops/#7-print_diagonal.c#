#include "main.h"
/**
 * print_diagonal - print diagonal
 * @n: number of diaginal
 * Return: / if n greater than 0
 */
void print_diagonal(int n)
{
int i;
 char a;
if (n > 0)
{
for (i = 0; i < n; i++)
{
a = \;
_putchar(a);
}
_putchar('\n');
}
_putchar('\n');
}