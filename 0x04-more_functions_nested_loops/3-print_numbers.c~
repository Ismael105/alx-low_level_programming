#include "main.h"
#include <stdio>
/**
 *print_numbers - prints numbers form zero to nine
 *Return: 0 Always (Success)
 */
void print_numbers(void)
{
int i = 0;
for (i=0; i < 10; i++)
{
_putchar(i + '0');
_putchar("\n");
}
return (0);
}
