#include <stdio.h>
/**
 *main - Entry point
 *Return: Always 0 (Success)
 */
int main(void)
{
int a = 48, b;
while (a = 48)
{
for (b = a; b <= 50; b++)
{
putchar("%b, %b", a, b);
putchar(44);
putchar(32);
}}
putchar('\n');
return (0);
}
