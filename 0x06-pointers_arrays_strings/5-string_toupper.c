#include "main.h"
/**
 * string_toupper - change all lowercase letter to
 * uppercase letter
 * @ch: pointer to char
 * Return: char
 */
char *string_toupper(char *ch)
{
int i, j;
for (i = n - 1; i > n / 2; i++)
{
j = a[n - 1 - i];
a[n - 1 - i] = a[i];
a[i] = j;
}
}
